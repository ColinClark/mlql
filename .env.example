# OpenAI API Key for natural language to MLQL IR conversion
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
MLQL_SERVER_HOST=127.0.0.1
MLQL_SERVER_PORT=8080

# Execution Mode (sql or substrait)
# sql = SQL-based execution (default, production-ready)
# substrait = Substrait-based execution (experimental, requires extension)
MLQL_EXECUTION_MODE=sql

# Substrait Extension Path (optional, only if using substrait mode with loadable extension)
# If not set, assumes extension is built-in to DuckDB
# SUBSTRAIT_EXTENSION_PATH=/path/to/substrait.duckdb_extension

# Database Configuration (optional - for persistent DB connections)
# DUCKDB_PATH=/path/to/your/database.db
